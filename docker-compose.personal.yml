# Personal deployment entry point. docker-compose.yml is kept equivalent so
# existing `docker compose up -d` commands continue to work.
services:
  macrolens:
    build:
      context: .
      dockerfile: backend/Dockerfile
      target: runtime
    image: macrolens-etl:personal
    container_name: macrolens-etl
    ports:
      - "${HOST_BIND:-127.0.0.1}:${PORT:-8000}:8000"
    env_file:
      - path: .env
        required: false
    environment:
      DATA_DIR: /app/data
      INTERNAL_API_TOKEN: ${INTERNAL_API_TOKEN:-}
      FINNHUB_API_KEY: ${FINNHUB_API_KEY:-}
      MASSIVE_API_KEY: ${MASSIVE_API_KEY:-}
      NEWSAPI_API_KEY: ${NEWSAPI_API_KEY:-}
      GNEWS_API_KEY: ${GNEWS_API_KEY:-}
    volumes:
      - macrolens-data:/app/data
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  macrolens-data:
